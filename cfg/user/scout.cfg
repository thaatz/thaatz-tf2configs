// mastercomfig network
net_hitscan

// ----------------------------------------------------------------------------
// general class cfg [11 September 2011] , based on:
// Chris' competitive medic config
// v1.056 | 05 August 2011 | fakkelbrigade.eu/chris/configs/
// ----------------------------------------------------------------------------
// ----------------------------------------------------------------------------
// Binds:
// M1 = primary weapon
// M2 = secondary weapon
// M3 = melee weapon
// M4 = secondary fire on primary weapon

// ----------------------------------------------------------------------------
// If you want to automatically undo this when changing to another class, put
// this in the config for that class (obviously uncomment it first)

resetmedcfg

// (You might want to put it in your autoexec as well, because binds are
// retained between sessions)
//
// alias "resetmedcfg" "unbind "shift";bind "mouse1" "+attack";bind "mouse2" "+attack2";bind "mwheelup" "invprev";bind "mwheeldown" "invnext";unbind "mouse3";unbind "mouse4";bind "C" "voice_menu_3";-forward;echo "*** MEDIC CONFIG UNLOADED ***""
// resetmedcfg
//
// OR
//
// bind a key to it in your autoexec only like:
//
// alias "resetmedcfg" "unbind "shift";bind "mouse1" "+attack";bind "mouse2" "+attack2";bind "mwheelup" "invprev";bind "mwheeldown" "invnext";unbind "mouse3";unbind "mouse4";bind "C" "voice_menu_3";-forward;echo "*** MEDIC CONFIG UNLOADED ***""
// bind "p" "resetmedcfg"
// resetmedcfg
// ----------------------------------------------------------------------------

resetmedcfg

// ----------------------------------------------------------------------------
// Weapon aliases
// ----------------------------------------------------------------------------
alias +atk2 "slot2;+attack"
alias -atk2 "-attack"
alias +atk1 "slot1;+attack"
alias -atk1 "-attack"
alias +melee "slot3;+attack"
alias -melee "-attack"

// ----------------------------------------------------------------------------
// secondary fire
// ----------------------------------------------------------------------------
// alias +2ndatk2 "+attack2;slot2;dropitem"
// alias -2ndatk2 "-attack2"

alias +2ndatk1 "+attack2;slot1;dropitem"
alias -2ndatk1 "-attack2"

// ----------------------------------------------------------------------------
// Binding
// ----------------------------------------------------------------------------
bind mouse1 "+atk1"
bind mouse2 "+atk2"
bind mouse5 "+melee"
bind mouse4 "+2ndatk1"

//
//additional things
//
// Null-cancelling movement script
// (prevents you from pressing two opposing directions, which causes you to stop moving)

// bind w +mfwd
// bind s +mback
// bind a +mleft
// bind d +mright

alias +mfwd "-back;+forward;alias checkfwd +forward"
alias +mback "-forward;+back;alias checkback +back"
alias +mleft "-moveright;+moveleft;alias checkleft +moveleft"
alias +mright "-moveleft;+moveright;alias checkright +moveright"
alias -mfwd "-forward;checkback;alias checkfwd none"
alias -mback "-back;checkfwd;alias checkback none"
alias -mleft "-moveleft;checkright;alias checkleft none"
alias -mright "-moveright;checkleft;alias checkright none"
alias checkfwd none
alias checkback none
alias checkleft none
alias checkright none
alias none ""

// Print to console
echo "*** general class cfg ***"